export interface WorkItem {
  title: string;
  description: string;
  tags: string[];
  result: string;
}

export interface TimelineEntry {
  year: string;
  title: string;
  company: string;
  description: string;
}

export interface PhilosophyPillar {
  title: string;
  description: string;
}
